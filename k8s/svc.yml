apiVersion: v1
kind: Service
metadata:
  name: mysql-svc
  namespace: bankapp
spec:
  selector:
    app: mysql
  ports:
    - port: 3306

---
apiVersion: v1
kind: Service
metadata:
  name: bankap-svc
  namespace: bankapp
spec:
  type: NodePort
  selector:
    app: bankapp
  ports:
    - port: 8080
      targetPort: 8080
